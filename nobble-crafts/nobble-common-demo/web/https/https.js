const https = require('https');

const server = (app, key, certificate, pass) => {
    throw new Error("This is a demo version and does not include 'SSL' support. Lol");
}

module.exports = { server };